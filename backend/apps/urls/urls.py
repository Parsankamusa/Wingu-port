from django.urls import path, include

# This file can be used for including all API versions or other URL patterns
# Currently all URLs are directly included in the main config/urls.py

urlpatterns = [
    # Include any app-specific URLs here if needed in the future
]
